#include <stdio.h>
#include <stdlib.h>

 int lista[10];
 int i;

 for(i=i<0;i<10;i=i+1){
	 Printf("Digite o numero para calcular%d:",i);
	 scanf("%d",&lista[i]);
	 lista[i]=lista[i]*2;
 }
printf("Valores dobrados:\n");
for(i=0;i=10;i=i+1){
	printf("Posicao%d: %d\n",i,lista[i];
}
return 0;
}

